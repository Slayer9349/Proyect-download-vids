version: '3.8'

services:
  bunkr-downloader:
    build: .
    container_name: bunkr-downloader
    ports:
      - "5000:5000"
    volumes:
      - ./Downloads:/app/Downloads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
